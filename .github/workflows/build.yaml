# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Build
on:
  push:
  pull_request:
    branches:
      - main
  workflow_call:
    inputs:
      build-config:
        description: >
          The build configurations to build, as a comma-separated list.

          Valid options are:
           * debug
           * release
        type: string
        required: true
      build-os:
        description: >
          The operating system to build for, as a comma-separated list.

          Valid options are:
           * linux
           * windows
           * macos
        type: string
        required: true
    outputs:
      artifact-id:
        description: >
          The ID of the artifact generated by this workflow.
        value: ${{jobs.build.outputs.artifact-id}}
      artifact-name:
        description: >
          The name of the artifact generated by this workflow.
        value: ${{jobs.build.outputs.artifact-name}}
    secrets:
      github-token:
        description: >
          The token to use when making authenticated GitHub API calls.
        required: true
jobs:
  generate-build-matrix:
    name: Generate build matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{steps.generate-build-matrix.outputs.result}}
    steps:
      - id: generate-build-matrix
        name: Generate build matrix
        env:
          BUILD_OS: ${{inputs.build-os}}
          BUILD_CONFIG: ${{inputs.build-config}}
        uses: actions/github-script@v7
        with:
          script: |
            let {BUILD_OS, BUILD_CONFIG} = process.env;
            if (!BUILD_OS) {
              BUILD_OS = 'windows,linux,macos';
            }
            if (!BUILD_CONFIG) {
              BUILD_CONFIG = 'debug,release';
            }
            const buildOSes = BUILD_OS.split(',').map((e) => e.trim()).map((e) => {
              switch (e) {
                case 'windows':
                  return 'windows-latest';
                case 'linux':
                  return 'ubuntu-latest';
                case 'macos':
                  return 'macos-latest';
              }
            });
            const buildConfigs = BUILD_CONFIG.split(',').map((e) => e.trim()).map((e) => {
              switch (e) {
                case 'debug':
                  return 'Debug';
                case 'release':
                  return 'Release';
              }
            });
            return {
              'build-config': buildConfigs,
              'build-os': buildOSes
            };
  build:
    name: Build
    runs-on: ${{matrix.build-os}}
    env:
      NUGET_PACKAGES: ${{github.workspace}}/.nuget/packages
    needs:
      - generate-build-matrix
    outputs:
      artifact-id: ${{steps.upload-artifacts.outputs.artifact-id}}
      artifact-name: ${{steps.upload-artifacts.outputs.artifact-name}}
    strategy:
      fail-fast: true
      matrix: ${{fromJson(needs.generate-build-matrix.outputs.matrix)}}
    steps:
      - id: checkout-repository
        name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{secrets.github-token || secrets.GITHUB_TOKEN}}
          lfs: true
      - id: setup-dotnet
        name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x
          cache: true
          cache-dependency-path: '**/packages.lock.json'
      - id: restore-dependencies
        name: Restore dependencies
        run: dotnet restore --locked-mode --use-current-runtime
      - id: build
        name: Build
        run: dotnet build --no-restore --configuration ${{matrix.build-config}} --use-current-runtime
      - id: generate-distribution
        name: Generate distribution
        run: dotnet publish --no-build --configuration ${{matrix.build-config}} --use-current-runtime --output dist Glekcraft/Glekcraft.csproj
      - id: upload-artifacts
        name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          path: dist
          name: dist-${{startsWith(matrix.build-config, 'Debug') && 'debug' || startsWith(matrix.build-config, 'Release') && 'release' || 'unknown'}}-${{startsWith(matrix.build-os, 'windows') && 'windows' || startsWith(matrix.build-os, 'ubuntu') && 'linux' || startsWith(matrix.build-os, 'macos') && 'macos' || 'unknown'}}
